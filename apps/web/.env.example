# Origin of the TurboPaste server (Hono / tRPC / REST).
# Baked in at build time, to change in production, rebuild.
VITE_SERVER_URL=http://localhost:3000

# Origin of the Starlight docs site. The /docs route in the web app redirects here.
VITE_DOCS_URL=http://localhost:4321
